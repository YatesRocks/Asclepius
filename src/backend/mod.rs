// TODO: Make the backend

/*
```rust
    struct Backend {
        ...fields
    }

    struct Client {
        ...fields
    }

    impl Backend {
        pub fn new() -> Self {}

        pub fn add_client(&mut self, client: Client) {}
        // return a client that you can edit
        pub fn edit_client(&mut self, client_id: u32) -> &mut Client {}
        // Store the database into a file (hint: serde)
        pub fn store(&self) {}
        ...methods
    }

    ... whatever else you need
```
*/
